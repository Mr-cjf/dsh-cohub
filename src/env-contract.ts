// dsh-port/src/env-contract.ts —— 执行器环境契约默认文本（通用原则 + 探测式自适应）
//
// 跨环境通用：本文本【不写死】任何单一环境的事实（例如「必须 await import()」「禁止 require」
// 「单次执行上限 600s」等）。具体执行器的模块加载方式、可用 API、超时上限与副作用，由子代理
// 在运行现场用「最小探针」探测确认；部署方也可通过 cohub.delegateEnvContract.text 覆盖为
// 已知的部署契约（见优化方案 v2 §3 环境假设矩阵 A1-A9 的探测/配置/降级三态）。

export const DEFAULT_ENV_CONTRACT = `执行器环境契约（通用原则 + 探测式自适应）

通用原则：
1. 自包含：你的每一条执行指令都应携带完成任务所需的全部信息（目标、路径/清单、约束、输出格式），不依赖父会话上下文。
2. 先验证再批量：对不确定的写法或 API，先跑最小探针验证通过，再批量执行；不要一次性把整批操作压进一个执行单元。
3. 错误可回传：任何执行失败都要把原始错误信息（消息/调用栈/退出码）原样带回，用于判断与续跑。

探测流程（当你不确定执行器的环境语义时）：
1. 用最小探针依次尝试候选写法（如不同的模块加载方式、API 调用方式），记录哪种写法通过；
2. 以通过验证的写法为准执行后续步骤；
3. 若所有候选写法都失败，立即停止并把探针错误原样回传，不要盲目重试或猜测。`;
